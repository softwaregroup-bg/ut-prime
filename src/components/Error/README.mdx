# Error
import { Canvas, Story, ArgsTable } from "@storybook/addon-docs";

Shows modal error message.

Do not use it directly, as it is connected
to the Redux state.

<Canvas>
    <Story id="error--basic" />
</Canvas>

## Properties

<ArgsTable story="."/>
